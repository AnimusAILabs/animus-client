# Kong JWT Credentials & Animus Config
# These are used BY THE AUTH SERVER to generate the JWT for the Animus API
CLIENT_ID=your-kong-consumer-jwt-key-or-client-id
CLIENT_SECRET=your-kong-consumer-jwt-secret # Used for HS256 signing
JWT_AUDIENCE=https://api.animusai.co # Target API audience for 'aud' claim
JWT_EXPIRES_IN=15m # JWT expiration time
ORG_ID=your-animus-org-id # Your Animus organization ID for 'org_id' claim

# Optional: Port for the auth server
# PORT=3001